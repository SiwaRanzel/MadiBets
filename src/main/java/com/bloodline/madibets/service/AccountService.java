package com.bloodline.madibets.service;

/**
 * Owner: Kieran (B-series). Agree this API in week 1 — Jason's leaderboard and
 * Siwapiwe's register both call into it.
 */
public class AccountService {
    // grantMonthlyAllowance()              -> 100 MadiBucks per user (business rule)
    // settleBet(int betID)                 -> B600: pay winners, log Transaction
    // getBalance(int userID)               -> used by register confirmation + leaderboard
}
