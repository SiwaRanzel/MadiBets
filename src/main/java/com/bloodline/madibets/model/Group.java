package com.bloodline.madibets.model;

/** Owner: Pieter (D-series). TODO: add fields from the Group table + getters/setters. */
public class Group {
    private int groupID;
    // TODO: groupName, description, createdBy, createdDate
}
