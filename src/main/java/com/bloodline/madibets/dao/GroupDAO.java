package com.bloodline.madibets.dao;

/** Owner: Pieter (D-series). */
public class GroupDAO {
    // D100 Create Group (CREATE)   -> create(...)
    // D200 View Group (READ)       -> findById(int groupID) / searchByName(String)
    // D300 Join Group (CREATE)     -> addMember(int groupID, int userID)
    // TODO: implement, starting with the READ.
}
