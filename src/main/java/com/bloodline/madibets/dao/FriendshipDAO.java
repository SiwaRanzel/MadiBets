package com.bloodline.madibets.dao;

/** Owner: Jason (C-series). */
public class FriendshipDAO {
    // C100 View Friends (READ)     -> findFriends(int userID)
    // C200 Add Friend (CREATE)     -> sendRequest(int from, int to)
    // C300 Remove Friend (DELETE)  -> remove(int friendshipID)
    // TODO: implement, starting with the READ.
}
